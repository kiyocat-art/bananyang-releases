// Re-export the registry and tab router for consumers that prefer
// importing from a dedicated module (e.g., workspaceTabsStore, useAutoSave).
export { canvasStoreRegistry, canvasTabRouter } from './canvasStore';
